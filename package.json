{
  "private": true,
  "packageManager": "npm@11.6.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --continue",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express-rate-limit": "^8.3.1",
    "html5-qrcode": "^2.3.8",
    "recharts": "^3.7.0""turbo": "^2.8.17"
  }
}